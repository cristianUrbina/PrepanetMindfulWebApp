extends layout

block content
  h1 Registrar alumnos a oferta
  p En el siguiente formulario ingrese el archivo que pude descargar de la ruta "/solicitudes"
  form(action="/registrar-alumnos" method="POST" enctype="multipart/form-data")
    input(type="file" name="csv" required)
    input(type="submit" value="Subir archivo")
  if message.length > 0
    div.error
     p #{message} 
