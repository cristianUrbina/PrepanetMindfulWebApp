extends layout

block content
  h1 Actualizar el estatus de cada alumno 
  p Debe descargar el reporte y modificar el estatus en cada celda 
  form(action="/actualizar-estatus" method="POST" enctype="multipart/form-data")
    input(type="file" name="csv" required)
    input(type="submit" value="Subir archivo")
  if message.length > 0
    div.error
     p #{message}  
