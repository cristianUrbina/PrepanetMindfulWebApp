extends layout
block content
  h1 Registrar alumnos a la aplicación
  p Se generan cuentas y registros de los alumnos y sus respectivos coordinadores
  form(action="/registrar-cuentas" method="POST" enctype="multipart/form-data")
    input(type="file" name="csv" required)
    input(type="submit" value="Subir archivo")
  if message.length > 0
    div.error
     p #{message} 
