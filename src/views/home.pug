extends layout
block content
  h1 Prepanet Mindful Web App
  if user
    p Nombre: #{user.nombre_completo} 
    p Correo: #{user.email} 
    p Nómina: #{id} 
    p Campus: #{user.campus} (#{user.id_campus})
